
set(io_src  "read_file.cpp" "ObjReader.cpp")

find_package(tinyobjloader)
set(symbol_libs)

link_libraries(tinyobjloader)
set(target_libs  Geometry Logger)
ADD_MACHI_LIBRARY(IO "${io_src}" "${target_libs}" "${symbol_libs}")
TARGET_CONFIGURE_SETUP(IO "IO")
